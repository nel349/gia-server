{
    "version": 2,
    "buildCommand": "bun install && bun run build",
    "outputDirectory": "dist",
    "framework": "bun",
    "routes": [
      {
        "src": "/(.*)",
        "dest": "app.js"
      }
    ]
  }